import {css} from 'styled-components';

export const proposalText = css`
  font-size: 25px;
  color: #ffbbbb;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  -webkit-background-clip: text;
  background-image: linear-gradient(
    -90deg,
    #ff3f04 0%,
    #ffffff 90%,
    #ffffff 100%
  );
`;
